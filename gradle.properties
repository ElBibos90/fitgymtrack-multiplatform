# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# === JVM SETTINGS ===
# Specifies the JVM arguments used for the daemon process.
# Increased memory for multiplatform builds
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# === GRADLE OPTIMIZATIONS ===
# Enable Gradle parallel builds for faster compilation
org.gradle.parallel=true
# Enable configuration caching for better performance  
org.gradle.configuration-cache=true
# Enable build caching
org.gradle.caching=true

# === ANDROID SETTINGS ===
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
android.useAndroidX=true
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# === KOTLIN SETTINGS ===
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enable new Kotlin/Native memory model
kotlin.native.binary.memoryModel=experimental
# Enable incremental compilation
kotlin.incremental=true

# === COMPOSE MULTIPLATFORM SETTINGS ===
# Enable Compose Compiler metrics (for debugging performance)
# compose.compiler.enableMetrics=true
# compose.compiler.enableReports=true

# === MULTIPLATFORM SETTINGS ===
# Enable Kotlin Multiplatform Mobile shared cache
kotlin.mpp.androidSourceSetLayoutVersion=2
# Suppress warnings about multiplatform unstable features
kotlin.mpp.enableCInteropCommonization=true

# === PERFORMANCE SETTINGS ===
# Enable Kotlin daemon for faster compilation
kotlin.daemon.jvm.options=-Xmx2048m